GEMINI_API_KEY=your_key_here
ADMIN_PASSWORD=viraliq-admin

# Secret used to sign auth JWTs. Generate a strong random value:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=change_me_to_a_long_random_string

# Comma-separated list of allowed frontend origins for CORS.
# Local dev default is http://localhost:3000. In production set this to your
# deployed frontend URL, e.g. https://viraliq.vercel.app
ALLOWED_ORIGINS=http://localhost:3000

# Number of trusted reverse-proxy hops in front of the app. Used to read the
# REAL client IP from X-Forwarded-For for rate limiting / brute-force guards.
# Railway routes through exactly one edge proxy, so the default of 1 is correct.
# Only raise it if you add more trusted proxies in front of Railway; setting it
# too high would trust spoofable client-supplied XFF entries again. (Default: 1)
# TRUSTED_PROXY_HOPS=1

# Database connection.
# Local dev: leave unset (defaults to sqlite+aiosqlite:///./viraliq.db)
# Production (Neon): paste your DIRECT connection string here (not the pooler URL).
#   Get it from: Neon console → your project → Connection Details → Direct
# DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require

# ── Transactional email (password reset, email verification, welcome) ──
# Railway blocks ALL outbound SMTP ports, so PRODUCTION must use Brevo's HTTP API.
# Get the key from Brevo → SMTP & API → API Keys (starts with "xkeysib-").
# Without it (and without SMTP below) signup still works, but no code is emailed.
# BREVO_API_KEY=xkeysib-...
# The "From" address must be a Brevo-verified sender.
EMAIL_FROM=CraftLint <you@example.com>
# Where email links point (welcome email button, etc.).
FRONTEND_URL=https://surge-chi-khaki.vercel.app
# SMTP is the LOCAL-DEV fallback only (used when BREVO_API_KEY is unset).
# SMTP_HOST=smtp-relay.brevo.com
# SMTP_PORT=587
# SMTP_USER=...@smtp-brevo.com
# SMTP_PASS=...

# ── Stripe billing (CraftLint Pro, $9.99/mo) ──
# Configured-OFF by default: without these the billing routes return 503 and the
# rest of the app is unaffected. Get keys from the Stripe dashboard (use the TEST
# keys — sk_test_… / whsec_… — until you've tested end to end, then swap to live).
#   STRIPE_SECRET_KEY   – Developers → API keys → Secret key (sk_test_… / sk_live_…)
#   STRIPE_PRICE_ID     – the recurring $9.99/mo price's ID (price_…)
#   STRIPE_WEBHOOK_SECRET – Developers → Webhooks → your endpoint → Signing secret (whsec_…)
# The publishable key is NOT needed: we redirect to Stripe-hosted Checkout.
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PRICE_ID=price_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# Comp / owner accounts: these emails get Pro (unlimited) for free, no payment,
# no Stripe. Comma-separated, case-insensitive. Use only addresses YOU control.
# COMP_PRO_EMAILS=you@example.com,tester@example.com

# ── Outcome collection: public post metrics (24h/7d/30d snapshots) ──
# TikTok uses keyless tikwm.com (no env var). It rate-limits datacenter IPs
# aggressively, so from Railway a bounded retry/backoff is applied and a terminal
# block is recorded with its HTTP status; if tikwm blocks the egress IP entirely,
# the collector will report failing on /health/collectors.
# Instagram REQUIRES a provider key, or every Reel fetch fails with success=0:
#   RAPIDAPI_KEY     – primary Instagram like-count provider (RapidAPI)
#   RAPIDAPI_IG_HOST – override the RapidAPI host (default: instagram-reels-downloader-api.p.rapidapi.com)
#   RAPIDAPI_IG_PATH – override the RapidAPI path (default: /download)
#   HIKERAPI_KEY     – fallback Instagram provider (HikerAPI), also used by IG harvest
# These MUST be set in the Railway prod environment — .env.example alone does nothing.
# RAPIDAPI_KEY=...
# RAPIDAPI_IG_HOST=instagram-reels-downloader-api.p.rapidapi.com
# RAPIDAPI_IG_PATH=/download
# HIKERAPI_KEY=...

# ── Google sign-in (optional) ──
# OAuth client ID from Google Cloud Console (…apps.googleusercontent.com).
# When unset, /api/auth/google returns 503 and the frontend hides the button —
# email/password signup is unaffected. The SAME value goes to the frontend as
# NEXT_PUBLIC_GOOGLE_CLIENT_ID.
# GOOGLE_CLIENT_ID=...apps.googleusercontent.com
